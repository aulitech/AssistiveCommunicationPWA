# Peri is meant to be found. Someone looking for a free communication aid
# should be able to reach it without being sent a link first.
#
# Kept as an explicit file rather than deleted: a missing robots.txt is a 404
# on every crawl, and this states the intent where the next person will look.
User-agent: *
Allow: /
